/* globals describe, it, expect */
'use strict';

// jasmine.getFixtures().fixturesPath = 'base/test/fixtures/';
// jasmine.getFixtures().preload('foo.html');

var foo = require('../index.js');

describe('foo', function() {

  it('should...', function() {
    expect(foo).not.toBe(undefined);
  });

});
